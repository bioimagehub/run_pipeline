name: merge_channels
channels:
  - conda-forge
dependencies:
  - python=3.12  # Specify your desired Python version
  - numpy
  - tqdm
  - matplotlib
  - nd2reader
  - pyyaml  # For the yaml import
  - joblib
  - pip
  - pip:
      - bioio
      - bioio-bioformats
      - bioio_ome_tiff
      - bioio-tifffile
      - bioio_nd2